{
  "name": "seruni",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "fmt": "turbo run fmt",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@praha/byethrow-docs": "^0.10.0",
    "turbo": "^2.8.2",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@10.26.1",
  "engines": {
    "node": ">=24"
  },
  "pnpm": {
    "overrides": {
      "node-fetch": "^3.0.0",
      "cross-fetch": "link:./packages/cross-fetch"
    }
  }
}
