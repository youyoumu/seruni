{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "globalEnv": ["ELECTRON_BIN"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"]
    },
    "build:noclean": {
      "dependsOn": ["^build:noclean"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck", "@repo/preload#build:noclean"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
